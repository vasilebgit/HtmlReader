import { createTheme } from '@mui/material/styles';

// Creează o temă MUI personalizată (opțional)
const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
  },
});

export default theme;